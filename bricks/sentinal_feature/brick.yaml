name: sentinal_feature
description: A new brick created with the Mason CLI.

# The following defines the brick repository url.
# Uncomment and update the following line before publishing the brick.
# repository: https://github.com/my_org/my_repo

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0+1

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ">=0.1.0-dev.60 <0.1.0"

vars:
  name:
    type: string
    description: The name of your feature.
    default: new_sentinal_feature
    prompt: What is your feature name?
  
  generate_tests:
    type: boolean
    description: Whether or not to generate test directory for this feature.
    default: true
    prompt: Do you want to generate the test folder directory?
